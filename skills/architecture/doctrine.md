# Architecture

The rules are in [../rules/code-structure.md](../rules/code-structure.md). This file does not restate them. Apply them on every non-trivial change. Do not invent a weaker checklist, and do not wait for someone to invoke `/architecture`.

Examples: [examples.md](examples.md).
