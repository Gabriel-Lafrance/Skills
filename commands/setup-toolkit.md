---
name: setup-toolkit
description: >-
  Install the pack AGENTS.md into the repo (default) or user level, with
  one-line pointers for harnesses that read another file, then optionally
  add ESLint, Prettier, and editor workspace files from this pack's
  templates. Use when the user wants the engineering toolkit, AGENTS.md,
  lint, format, eslint, prettier, or editor extensions in an app.
---

# Setup toolkit

Follow the `/setup-toolkit` skill in this pack (`skills/setup-toolkit/SKILL.md`).

Apply `rules/code-quality.md` and `rules/code-structure.md` this turn, then execute that skill. The skill installs the contract. This command does not grow a second copy of those steps. Do not invent a different ESLint or Prettier stack.
